name: wp-installer
project_files:
  - commands/web/wp-install
  - commands/web/wp-test-content
post_install_actions:
  - chmod +x commands/web/wp-install
  - chmod +x commands/web/wp-test-content
  - echo "✅ Installed 'wp-install' and 'wp-test-content'. Run 'ddev wp-install' to start."
removal_actions:
  - rm -f commands/web/wp-install
  - rm -f commands/web/wp-test-content
# ddev_version_constraint: ">= v1.23.0"
